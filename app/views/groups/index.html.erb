<h1>Groups</h1>
<%= link_to "New", new_group_path, :class => "btn btn-primary" %></p>
<br />

<ul>
  <% @groups.each do |group| %>
    <li>
		<%= link_to group.name, group_path(group), :class=>"view-link" %> |
		<%= link_to "Edit", edit_group_path(group) %> |
		<%= link_to "Delete", group_path(group), :method => :delete %> 
    </li>
  <% end %>
</ul>
