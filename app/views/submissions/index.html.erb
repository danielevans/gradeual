<h1><%= resource_class.to_s.pluralize.humanize %></h1>
<p> <%= link_to "New", new_resource_path, :class => "btn btn-primary" %></p>
<br />

<ul>
  <% collection.each do |r| %>
    <li>
    <%= link_to r.to_s, resource_path(r) %>
    </li>
  <% end %>
</ul>
